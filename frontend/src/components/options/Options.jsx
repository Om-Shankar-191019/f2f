import React from "react";

const Options = () => {
  return (
    <div className="h-screen hidden md:w-16 border-2 border-red-600 md:flex flex-col">
      opt
    </div>
  );
};

export default Options;
